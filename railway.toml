[services.backend]
[services.backend]
command = "cd Backend && npm install && npx prisma generate"
start = "cd Backend && npx prisma generate && npm run start"



[services.frontend]
command = "cd Frontend && npm install && npm run build" 
start = "cd Frontend && npm start"
