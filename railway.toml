[services.backend]
  command = "cd Backend && npm install && npm install node-cron"
  start = "cd Backend && npm run start"


[services.frontend]
  command = "cd Frontend && npm install && npm run build"
  start = "cd Frontend && npm start"
