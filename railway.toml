[services.backend]
command = "cd Backend && rm -rf node_modules && npm install && npm run generate:prisma"
start = "cd Backend && npm run start"


[services.frontend]
command = "cd Frontend && npm install && npm run build" 
start = "cd Frontend && npm start"
